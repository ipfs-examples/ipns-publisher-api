on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    name: build
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v1
    - name: install node
      uses: actions/setup-node@v1
      with:
        node-version: 18
    - name: npm install
      run: npm install
    - name: tsc
    - npm run build

